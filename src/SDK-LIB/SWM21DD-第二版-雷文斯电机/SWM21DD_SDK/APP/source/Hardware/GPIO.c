
/* --------------------------- (C) COPYRIGHT 2022  -----------------------------
    File Name      : main.c
    Author         : Bryce Lin
    Version        : V1.0
    Date           : 2022-07-28
    Description    : This file contains XX-XX-XX function used for Motor Control.
----------------------------------------------------------------------------------------------------  
                                       All Rights Reserved
------------------------------------------------------------------------------------------------- */
#include "MyProject.h"
#include "SWM211.h"
#include "GPIO.h"

void Init_GPIO(void)
{	
	// GPIO_Init(GPIOB, PIN0, input, no_pull_up, pull_down, 0);
	// GPIO_Init(GPIOM, PIN9, input, no_pull_up, pull_down, 0);
	// GPIO_Init(GPIOA, PIN8, input, no_pull_up, pull_down, 0);
	// GPIO_Init(GPIOB, PIN9, input, no_pull_up, pull_down, 0);

  // debug IO
  // GPIO_Init(DEBUG_GPIO_PORT, DEBUG_GPIO_PIN, output, no_pull_up, no_pull_up, 0);
}









